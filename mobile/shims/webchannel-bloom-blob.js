// Shim for @firebase/webchannel-wrapper/bloom-blob
// This subpath is not available in the React Native bundle.
// Firestore works fine without it (it's used for bloom filter cache optimisation).
module.exports = {};
