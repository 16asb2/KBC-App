name = "kbc-admin-token"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Non-secret config — safe to commit
[vars]
FIREBASE_PROJECT_ID = "kbc-app-dev"

# Secrets — set via CLI, never committed:
#   wrangler secret put FIREBASE_API_KEY
#   wrangler secret put GOOGLE_ADMIN_CLIENT_ID
#   wrangler secret put GOOGLE_ADMIN_CLIENT_SECRET
#   wrangler secret put GOOGLE_ADMIN_REFRESH_TOKEN
